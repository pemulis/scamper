# requirements.txt

# FastAPI + Uvicorn for webserver
fastapi
uvicorn

# For environment variables
python-dotenv

# For request body parsing (used internally by FastAPI, but good to list explicitly)
pydantic

# Coinbase AgentKit libraries
coinbase_agentkit
coinbase_agentkit_openai_agents_sdk

# Pin openai version
openai==1.66.0
